{% extends '@OHMediaAlert/alert_form.html.twig' %}

{% set form_title = 'Create Alert' %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb(bootstrap_icon('exclamation-triangle') ~ ' Alerts', 'alert_index'),
  bootstrap_breadcrumb('Create'),
) }}
{% endblock %}
